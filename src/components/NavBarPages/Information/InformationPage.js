import React from 'react';
import styled from 'styled-components';
import { Link } from 'react-router-dom';

const Title = styled.h1`
	color: black;
`;

export const Information = () => {
	return <Title>Information</Title>;
};
